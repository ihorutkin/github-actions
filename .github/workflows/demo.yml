name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Printe to console
        run: echo Hello github actions
      - name: Print a few lines
        run: |
          echo First line
          echo Second line
